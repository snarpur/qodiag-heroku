chart_name: Column
chart_type: Column
extends: Chart
chart_config:
  chart:
    type: column
  tooltip:
    enabled: false
    useHTML: true
    footerFormat: '</table>'
    shared: true
  xAxis:
    labels:
      useHTML: true
  plotOptions:
    column:
      cursor: pointer
      point:
        events:
          click: drillToQuestion
    # formatter: true
