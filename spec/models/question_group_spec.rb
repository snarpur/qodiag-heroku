require 'spec_helper'
describe QuestionGroup do

  # describe "scopes select distinct" do

  #   before do
  #     @names = ['first_group','second_group']
  #     @names.each do |name|
  #       2.times {Factory(:question_group, :text => name)}
  #     end
  #   end

  #   it "should list all question group names once" do
  #     # QuestionGroup.names.length.should == @names.length
  #     # QuestionGroup.names.last.should_not == QuestionGroup.names.first
  #   end

  # end

  # describe "scopes with questions" do
  #   before(:each) do
  #     @names = ['first_group','second_group']
  #     @questions = %w{what how is do}
  #     @names.each do |name|
  #       group = Factory(:question_group, :text => name)
  #       @questions.each do |q|
  #         question = Factory(:question, :text => q, :question_group => group)
  #       end
  #     end
  #   end
  #   it "does something" do

  #   end

  # end
end