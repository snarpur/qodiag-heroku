require 'spec_helper'

describe "entry_set_responses/edit" do
  # before(:each) do
  #   @entry_set_response = assign(:entry_set_response, stub_model(EntrySetResponse,
  #     :entry_set_id => 1,
  #     :responder_item_id => 1
  #   ))
  # end

  # it "renders the edit entry_set_response form" do
  #   render

  #   # Run the generator again with the --webrat flag if you want to use webrat matchers
  #   assert_select "form[action=?][method=?]", entry_set_response_path(@entry_set_response), "post" do
  #     assert_select "input#entry_set_response_entry_set_id[name=?]", "entry_set_response[entry_set_id]"
  #     assert_select "input#entry_set_response_responder_item_id[name=?]", "entry_set_response[responder_item_id]"
  #   end
  # end
end
