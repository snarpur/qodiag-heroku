require 'spec_helper'

describe "entry_set_responses/show" do
  # before(:each) do
  #   @entry_set_response = assign(:entry_set_response, stub_model(EntrySetResponse,
  #     :entry_set_id => 1,
  #     :responder_item_id => 2
  #   ))
  # end

  # it "renders attributes in <p>" do
  #   render
  #   # Run the generator again with the --webrat flag if you want to use webrat matchers
  #   rendered.should match(/1/)
  #   rendered.should match(/2/)
  # end
end
