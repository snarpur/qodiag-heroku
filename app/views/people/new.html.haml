=javascript :defaults, 'delegate'
%div.family-registration
  =render "shared/wizard_navigation"
  - if @wizard.current_step == "patient"
    = render 'patient'
  - else 
    = render "parent"
  = link_to 'Back', people_path
