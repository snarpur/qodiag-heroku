%ul.login
  -if user_signed_in?
    %li
      %em="#{t('devise.sessions.signed_in_as')}:"
      %i="#{current_user.email}"
    %li
      %a{:href => destroy_user_session_path}
        = t('actions.sign_out')
  -else
    %li
      %a{:href => new_user_session_path}
        = t('actions.log_in')