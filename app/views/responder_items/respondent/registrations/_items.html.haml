%h4= t("responder_item.status.#{item[:name]}.header").capitalize
%table.table
  %thead
    %tr
      %th Heiti
      %th Beiðni send dags.
      %th{:colspan => 2}= t("responder_item.status.#{item[:name]}.column").capitalize
  %tbody
    = render :partial => user_partial_path("registrations/item"), :collection => item[:items]
