%p
  = t('devise.mailer.confirmation_instructions.greeting', name: @resource.email) 
%p
  = t('devise.mailer.confirmation_instructions.body')[0]
%p= link_to t('devise.mailer.confirmation_instructions.links.confirm'), confirmation_url(@resource, :confirmation_token => @resource.confirmation_token)
