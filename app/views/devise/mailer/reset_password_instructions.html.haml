%p
  = t('devise.mailer.reset_password_instructions.greeting', name: @resource.email) 
%p 
  = t('devise.mailer.reset_password_instructions.body')[0]
%p= link_to t('devise.mailer.reset_password_instructions.link.change_password'), edit_password_url(@resource, :reset_password_token => @resource.reset_password_token)
%p 
  = t('devise.mailer.reset_password_instructions.body')[1]
%p 
  = t('devise.mailer.reset_password_instructions.body')[2]