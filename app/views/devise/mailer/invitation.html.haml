%p=t('devise.mailer.invitation.body.greeting', :name => @user.person.firstname)
%p
  =t('devise.mailer.invitation.body.text')
=link_to t('actions.sign_up'), accept_user_invitation_url(:invitation_token => @user.invitation_token)
%p
  =t('devise.mailer.invitation.body.regards')

