%p
  = t('devise.mailer.unlock_instructions.greeting', name: @resource.email)
%p 
  = t('devise.mailer.unlock_instructions.body')[0]
%p
  = t('devise.mailer.unlock_instructions.body')[1]
%p= link_to t('devise.mailer.unlock_instructions.link.unlock'), unlock_url(@resource, :unlock_token => @resource.unlock_token)
