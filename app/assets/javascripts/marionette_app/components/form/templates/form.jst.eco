<div class="panel radius">
	<div id="form-content-region"></div>
	<% if @footer: %>
		<footer>
			<% if @buttons: %>
				<% for button in @buttons: %>
					<button type="button" data-form-button="<%= button.buttonType %>" class="<%= button.className %>"><%= button.text %></button>
				<% end %>
			<% end %>
		</footer>
	<% end %>
</div>
